% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/math.R
\name{weighted_sum}
\alias{weighted_sum}
\title{Weighted sum with logical index}
\usage{
weighted_sum(x, w, idx)
}
\arguments{
\item{x}{Numeric vector of values.}

\item{w}{Numeric vector of weights, same length as \code{x}.}

\item{idx}{Logical vector indicating which elements to include.}
}
\value{
A single numeric value. Returns 0 if no TRUE values in \code{idx}.
}
\description{
Computes the weighted sum of \code{x} using weights \code{w}, where \code{idx} is TRUE.
}
\examples{
idx_active <- c(TRUE, TRUE, FALSE, FALSE, FALSE, TRUE)
AGSA <- c(0.1, 0.2, 0.03, 0.04, 0.05, 0.6)
Duration <- c(100, 90, 80, 70, 60, 50)
ActiveVolume <- weighted_sum(AGSA, Duration, idx_active)
}
\keyword{internal}
